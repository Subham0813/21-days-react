# Autocomplete Search App (React + Tailwind)

A simple autocomplete search bar built using **React**, **Tailwind CSS**, and **React Icons**.

### ✨ Main learning
- Debounced search suggestions, Implemented realusecase of debouncing.

## 🚀 Setup & Run Locally

### Clone the repository
```bash
git clone https://github.com/subham0813/Day01-searchbar-with-debouncing
```
- Install dependencies
```bash
npm install
```
- If Tailwind is not installed yet, run:
```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```
- Update tailwind.config.js like this:
```js
content: ["./src/**/*.{js,jsx,ts,tsx}"],
```
### Start Development Server
```bash
 npm run dev   # for Vite 
```
## or
```bash
npm start     # for CRA
```